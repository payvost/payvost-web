import { metadata as billPaymentsMetadata } from './metadata';

export const metadata = billPaymentsMetadata;

export default function BillPaymentsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}

