import { metadata as splitPaymentsMetadata } from './metadata';

export const metadata = splitPaymentsMetadata;

export default function SplitPaymentsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}

