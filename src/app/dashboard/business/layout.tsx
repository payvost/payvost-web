import { metadata as businessDashboardMetadata } from './metadata';

export const metadata = businessDashboardMetadata;

export default function BusinessDashboardPageLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}

