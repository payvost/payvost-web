import { metadata as supportMetadata } from './metadata';

export const metadata = supportMetadata;

export default function SupportLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}

