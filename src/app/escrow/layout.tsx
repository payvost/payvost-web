import { metadata as escrowMetadata } from './metadata';

export const metadata = escrowMetadata;

export default function EscrowLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}

