import { metadata as recurringPaymentsMetadata } from './metadata';

export const metadata = recurringPaymentsMetadata;

export default function RecurringPaymentsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}

